# AI Rescue USB - Dependencies
# Only packages actually imported by the code today. Hardware/disk/network
# tools (lspci, lsblk, parted, wget, testdisk, photorec, ddrescue, chntpw,
# clamav, ...) are invoked as system CLI commands via subprocess, not as
# pip packages — they must be present on the live boot system's PATH.

# Hardware detection
psutil==5.9.6

# Testing
pytest==7.4.0
pytest-cov==4.1.0

# Local LLM support is optional — see requirements-llm.txt. The engine
# falls back to a rule-based mode when it isn't installed.
